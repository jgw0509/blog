{% extends 'base.html' %}

{% block title %}게시글 목록{% endblock %}

{% block content %}
<!--
==================================================
게시글 목록 ?�플�?
==================================================
검??결과 ?�시
카테고리 ?�터
?�이지?�이??
==================================================
-->

<div class="container">
    <div class="row">
        <!-- 메인 콘텐�?-->
        <div class="col-lg-8">
            <!-- ?�이지 ?�더 -->
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h1 class="h3 mb-1">
                        <i class="bi bi-journals me-2"></i>
                        {% if current_category %}
                        {{ current_category }} 게시글
                        {% elif search_query %}
                        "{{ search_query }}" 검??결과
                        {% else %}
                        ?�체 게시글
                        {% endif %}
                    </h1>
                    <p class="text-body-secondary mb-0">
                        �?{{ page_obj.paginator.count }}개의 게시글
                    </p>
                </div>
                {% if user.is_authenticated %}
                <a href="{% url 'blog:post_create' %}" class="btn btn-primary">
                    <i class="bi bi-pencil-square me-1"></i>글?�기
                </a>
                {% endif %}
            </div>

            <!-- 검??�??�터 -->
            <div class="card shadow-sm mb-4">
                <div class="card-body">
                    <form action="{% url 'blog:post_list' %}" method="get" class="row g-3">
                        <div class="col-md-6">
                            <input type="search" name="q" class="form-control" placeholder="검?�어�??�력?�세??
                                value="{{ search_query }}">
                        </div>
                        <div class="col-md-3">
                            <select name="category" class="form-select">
                                <option value="">모든 카테고리</option>
                                {% for cat in categories %}
                                <option value="{{ cat.slug }}" {% if current_category == cat.slug %}selected{% endif %}>
                                    {{ cat.name }}
                                </option>
                                {% endfor %}
                            </select>
                        </div>
                        <div class="col-md-3">
                            <select name="sort" class="form-select">
                                <option value="recent" {% if current_sort == 'recent' %}selected{% endif %}>최신??/option>
                                <option value="likes" {% if current_sort == 'likes' %}selected{% endif %}>좋아?�순</option>
                            </select>
                        </div>
                        <div class="col-12">
                            <button type="submit" class="btn btn-primary w-100">
                                <i class="bi bi-search"></i> 검??�??�렬 ?�용
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- 게시글 목록 -->
            <div class="posts-list">
                {% for post in posts %}
                <article class="card shadow-sm mb-4 post-card">
                    <div class="row g-0">
                        {% if post.thumbnail %}
                        <div class="col-md-4">
                            <img src="{{ post.thumbnail.url }}" class="img-fluid rounded-start h-100"
                                alt="{{ post.title }}" style="object-fit: cover;">
                        </div>
                        {% endif %}
                        <div class="{% if post.thumbnail %}col-md-8{% else %}col-12{% endif %}">
                            <div class="card-body">
                                <div class="d-flex gap-2 mb-2">
                                    {% if post.category %}
                                    <a href="{% url 'blog:category_detail' post.category.slug %}"
                                        class="badge bg-primary text-decoration-none">
                                        {{ post.category.name }}
                                    </a>
                                    {% endif %}
                                    {% if not post.published %}
                                    <span class="badge bg-warning">비공�?/span>
                                    {% endif %}
                                </div>

                                <h2 class="card-title h5">
                                    <a href="{{ post.get_absolute_url }}"
                                        class="text-decoration-none text-body stretched-link">
                                        {{ post.title }}
                                    </a>
                                </h2>

                                <div
                                    class="d-flex justify-content-between align-items-center small text-body-secondary">
                                    <div>
                                        <a href="{% url 'accounts:profile' post.author.username %}"
                                            class="text-decoration-none text-body-secondary position-relative"
                                            style="z-index: 2;">
                                            <i class="bi bi-person me-1"></i>{{ post.author.username }}
                                        </a>
                                        <span class="mx-2">|</span>
                                        <i class="bi bi-calendar me-1"></i>{{ post.created_at|date:"Y.m.d" }}
                                    </div>
                                    <div class="d-flex align-items-center">
                                        <i class="bi bi-eye me-1"></i>{{ post.views }}
                                        <span class="mx-2">|</span>
                                        <i class="bi bi-chat me-1"></i>{{ post.get_comment_count }}
                                        <span class="mx-2">|</span>
                                        <i class="bi bi-heart-fill text-danger me-1"></i>{{ post.likes_count }}
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </article>
                {% empty %}
                <div class="alert alert-info">
                    <i class="bi bi-info-circle me-2"></i>
                    {% if search_query %}
                    "{{ search_query }}"???�??검??결과가 ?�습?�다.
                    {% else %}
                    게시글???�습?�다.
                    {% endif %}
                </div>
                {% endfor %}
            </div>

            <!-- ?�이지?�이??-->
            {% if page_obj.has_other_pages %}
            <nav aria-label="게시글 ?�이지?�이??>
                <ul class="pagination justify-content-center">
                    {% if page_obj.has_previous %}
                    <li class="page-item">
                        <a class="page-link"
                            href="?page=1{% if search_query %}&q={{ search_query }}{% endif %}{% if current_category %}&category={{ current_category }}{% endif %}">
                            <i class="bi bi-chevron-double-left"></i>
                        </a>
                    </li>
                    <li class="page-item">
                        <a class="page-link"
                            href="?page={{ page_obj.previous_page_number }}{% if search_query %}&q={{ search_query }}{% endif %}{% if current_category %}&category={{ current_category }}{% endif %}">
                            <i class="bi bi-chevron-left"></i>
                        </a>
                    </li>
                    {% endif %}

                    {% for num in page_obj.paginator.page_range %}
                    {% if page_obj.number == num %}
                    <li class="page-item active">
                        <span class="page-link">{{ num }}</span>
                    </li>
                    {% elif num > page_obj.number|add:'-3' and num < page_obj.number|add:'3' %} <li class="page-item">
                        <a class="page-link"
                            href="?page={{ num }}{% if search_query %}&q={{ search_query }}{% endif %}{% if current_category %}&category={{ current_category }}{% endif %}">{{
                            num }}</a>
                        </li>
                        {% endif %}
                        {% endfor %}

                        {% if page_obj.has_next %}
                        <li class="page-item">
                            <a class="page-link"
                                href="?page={{ page_obj.next_page_number }}{% if search_query %}&q={{ search_query }}{% endif %}{% if current_category %}&category={{ current_category }}{% endif %}">
                                <i class="bi bi-chevron-right"></i>
                            </a>
                        </li>
                        <li class="page-item">
                            <a class="page-link"
                                href="?page={{ page_obj.paginator.num_pages }}{% if search_query %}&q={{ search_query }}{% endif %}{% if current_category %}&category={{ current_category }}{% endif %}">
                                <i class="bi bi-chevron-double-right"></i>
                            </a>
                        </li>
                        {% endif %}
                </ul>
            </nav>
            {% endif %}
        </div>

        <!-- ?�이?�바 -->
        <div class="col-lg-4">
            <!-- 카테고리 -->
            <div class="card shadow-sm mb-4">
                <div class="card-header bg-transparent">
                    <h5 class="mb-0">
                        <i class="bi bi-folder me-2"></i>카테고리
                    </h5>
                </div>
                <div class="list-group list-group-flush">
                    <a href="{% url 'blog:post_list' %}"
                        class="list-group-item list-group-item-action {% if not current_category %}active{% endif %}">
                        ?�체
                    </a>
                    {% for cat in categories %}
                    <a href="{% url 'blog:category_detail' cat.slug %}" class="list-group-item list-group-item-action d-flex justify-content-between align-items-center
                                  {% if current_category == cat.slug %}active{% endif %}">
                        {{ cat.name }}
                        <span class="badge bg-secondary rounded-pill">{{ cat.get_post_count }}</span>
                    </a>
                    {% endfor %}
                </div>
            </div>

            <!-- ?�기 게시글 -->
            <div class="card shadow-sm">
                <div class="card-header bg-transparent">
                    <h5 class="mb-0">
                        <i class="bi bi-fire me-2"></i>?�기 게시글
                    </h5>
                </div>
                <div class="list-group list-group-flush">
                    {% for post in popular_posts %}
                    <a href="{{ post.get_absolute_url }}" class="list-group-item list-group-item-action">
                        <div class="d-flex justify-content-between">
                            <span class="text-truncate">{{ post.title }}</span>
                            <small class="text-body-secondary">
                                <i class="bi bi-eye"></i> {{ post.views }}
                            </small>
                        </div>
                    </a>
                    {% empty %}
                    <div class="list-group-item text-body-secondary">
                        ?�기 게시글???�습?�다.
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
